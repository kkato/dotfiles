# Shared Agent Guidance

- Respond in Japanese by default unless the user asks otherwise.
- Prefer `rg` for search and `jq` for JSON inspection.
- Ask before destructive commands or commands that mutate external services.
- Keep repository edits focused on the requested change.
