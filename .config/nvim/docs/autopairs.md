# nvim-autopairs

括弧や引用符を入力すると、対応する閉じ文字を自動挿入するプラグイン。

## 対応する文字

| 入力 | 自動挿入 |
|------|------|
| `(` | `)` |
| `{` | `}` |
| `[` | `]` |
| `"` | `"` |
| `'` | `'` |
| `` ` `` | `` ` `` |

## 動作

- 閉じ括弧の位置にカーソルがある状態で`)` を入力すると、重複せずに通過する
- 括弧内が空の状態で`<BS>`を押すと、ペアごと削除される
