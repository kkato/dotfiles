# Repository Guidance

- This repository manages personal dotfiles for macOS.
- Keep generated caches, histories, tokens, and machine-local state out of git.
- Prefer symlinks from `$HOME` into this repository instead of copying managed files.
- Update `install.sh` whenever adding a new managed dotfile path.
